/**
 * useAuth — wraps Supabase auth state.
 *
 * TODO: implement in Issue #3
 * - Expose user, profile, signIn, signOut
 * - Handle magic link flow
 * - Sync profile to profiles table on first login
 */
export function useAuth() {
  // Stub — to be implemented
  return { user: null, profile: null, loading: false }
}
